{{#each posts as |post|}}
<a href="/editpost/{{post.id}}">
    <div class="card">
        <div class="card-body d-flex flex-row justify-content-between">
            <p>{{post.title}}</p>
            <p>{{post.date}}</p>
        </div>
    </div>
</a>
{{/each}}

<a id="new-post-link" href="/post">+ New Post</a>